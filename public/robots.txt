User-agent: *
Disallow:

# Legacy /pgmfi/ and /pgmfi-archive/ serve X-Robots-Tag: noindex and are
# intentionally left crawlable so search engines can see the noindex and
# drop them from the index. Once Search Console shows them gone, they can
# be Disallowed again to save crawl budget.

Sitemap: https://www.hondabase.com/sitemap.xml
