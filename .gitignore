*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
# Canonical content lives in its own repo (hondabase/articles), cloned here as a working
# tree. Keep it out of the site repo; wire it as a submodule separately if desired.
/content
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Local Gemini API key (never commit)
.gemini_api_key

# Internal maintainer documentation (ignored by git)
docs/internal/

# Local scratch workspace and browser-automation debug output
/scratch
/.playwright-mcp

