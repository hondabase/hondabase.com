// Site-wide JS entry. Intentionally tiny: it ships on every page. The heavy rich-text editor lives
// in its own entry (resources/js/editor.js) and is loaded only on the /new and /edit pages, so a
// reader on a phone never downloads TipTap. Add only genuinely site-wide behaviour here.
